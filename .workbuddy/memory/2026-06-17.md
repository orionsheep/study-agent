## 24. 搬运原项目成熟组件替换简陋版本
- **用户反馈**：系统有 bug，英语工作区无法正常工作，要求直接搬运原项目 `english-word-fission` 的成熟组件。
- **诊断**：前端 TypeScript 编译通过，但组件过于简陋（我们自己写的简单版本）。后端 API 代理层正常工作（`/api/english/words?search=hello` 返回 `["hello"]`）。
- **搬运工作**：
  - 安装依赖：`@types/d3-force`（FissionGraph 需要），`react-force-graph-2d` 和 `@tanstack/react-virtual` 之前已安装。
  - **WordList**：完全重写，保留虚拟滚动、搜索框、英文单词列表。使用黑色主题（`#0a0a0a` 背景），适配 `/api/english/words` API。
  - **WordDetail**：完全重写，保留：单词标题、US/UK 发音按钮（有道词典 + TTS fallback）、音标、柯林斯星级、中文简明释义、Markdown 渲染（`react-markdown` + `rehype-raw` + `remark-breaks`）、词形变化、详细释义（含中英文例句）、近义词辨析、笔记区域。适配 `/api/english/words/{word}` API。
  - **FissionGraph**：完全重写，保留：粒子系统、力导向图（`react-force-graph-2d`）、控制按钮（刷新、缩放、二级节点显隐、设置面板）、图例、设置面板（节点大小、斥力、距离、碰撞等）。适配 `/api/english/fission?word={word}` API。支持 `dashboard` 和 `immersive` 两种模式。
  - **EnglishWorkspaceApp**：更新为 4 Tab 布局：
    - 单词列表 Tab：左侧 WordList（280px）+ 右侧 WordDetail（flex:1）
    - 裂变图 Tab：全屏 FissionGraph + 沉浸模式按钮
    - 测验 Tab：QuizPanel（保留之前版本）
    - AI 对话 Tab：AIChatPanel（保留之前版本，SSE 流式）
  - 支持键盘快捷键：J（US 发音）、K（UK 发音）、↑/↓（切换单词）
  - 支持沉浸模式：全屏裂变图覆盖层，带退出按钮。
- **TypeScript 编译**：全部通过，零错误。
- **构建**：`npm run build` 成功，2.14s。
- **文件清单**：
  - `apps/web/src/features/learning-apps/english/components/WordList.tsx`（重写）
  - `apps/web/src/features/learning-apps/english/components/WordDetail.tsx`（重写）
  - `apps/web/src/features/learning-apps/english/components/FissionGraph.tsx`（重写）
  - `apps/web/src/features/learning-apps/english/EnglishWorkspaceApp.tsx`（更新）

