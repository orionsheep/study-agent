# Resource Bundle Agent

File: `services/api/app/agents/resource_bundle_agent.py`

Generates coordinated document, mindmap, quiz, code, and reading resources through Skills. Resources carry source_refs and verifier results and can become Canvas Apps.

Tests: `test_resource_bundle_creates_at_least_five_resources`.
