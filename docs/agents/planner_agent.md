# Planner Agent

File: `services/api/app/agents/planner_agent.py`

Generates a personalized learning path from profile, mastery, and course graph context. Weak math evidence inserts prerequisite stages and the path writes learning-path memory.

Tests: `test_planner_inserts_prerequisite_stage_for_weak_math`.
